const v = await import('valibot'); const { orderSchema } = await import('./features/orders/ordersValidation.js'); console.log('Testing fixed validation...'); try { const result = v.parse(orderSchema, {cryptocurrencyid: 0, orderType: 'buy', orderVariant: 'limit', quantity: 2.5, price: 48000}); console.log('VALIDATION BROKEN - PASSED (should fail):', result); } catch (e) { console.log('VALIDATION WORKING - FAILED correctly:', e.message); }
